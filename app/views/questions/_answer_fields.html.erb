<%= form_for([@question, @question.answers.build]) do |f| %>
		<div class="field">
		<%= f.label :result %><br />
		<%= f.text_area :result %>
	</div>
		<div class="actions">
		<%= f.submit %>
	</div>
<% end %>
